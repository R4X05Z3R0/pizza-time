import org.junit.jupiter.api.Test;

class MenuTest {

    @Test
    void displayMenu() {
        Menu menu = new Menu();
        String expected = "";
       // assertEquals(expected, menu.displayMenu(););
    }
}